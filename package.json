{
  "name": "hitl-mcp",
  "displayName": "HITL MCP",
  "description": "MCP server for chatting with a human agent",
  "version": "0.0.22",
  "publisher": "3DTek-xyz",
  "icon": "HumanAgent_Icon_Square.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/nix1/HITL-MCP.git"
  },
  "bugs": {
    "url": "https://github.com/nix1/HITL-MCP/issues"
  },
  "engines": {
    "vscode": "^1.105.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "hitl-mcp.createSession",
        "title": "Create New Chat Session",
        "icon": "$(add)"
      },
      {
        "command": "hitl-mcp.refreshSessions",
        "title": "Refresh Sessions",
        "icon": "$(refresh)"
      },
      {
        "command": "hitl-mcp.showStatus",
        "title": "Show MCP Server Status"
      },
      {
        "command": "hitl-mcp.openChat",
        "title": "Open Chat Session"
      },
      {
        "command": "hitl-mcp.startServer",
        "title": "Start MCP Server",
        "icon": "$(play)"
      },
      {
        "command": "hitl-mcp.stopServer",
        "title": "Stop MCP Server",
        "icon": "$(stop)"
      },
      {
        "command": "hitl-mcp.restartServer",
        "title": "Restart MCP Server",
        "icon": "$(debug-restart)"
      },
      {
        "command": "hitl-mcp.reportIssue",
        "title": "Report Issue / Suggestion",
        "icon": "$(github)"
      }
    ],
    "viewsContainers": {
      "panel": [
        {
          "id": "hitl-mcp",
          "title": "HITL Chat",
          "icon": "$(comment-discussion)"
        }
      ]
    },
    "views": {
      "explorer": [
        {
          "id": "hitl-mcp.chatSessions",
          "name": "Chat Sessions",
          "when": "true",
          "icon": "$(comment-discussion)"
        }
      ],
      "hitl-mcp": [
        {
          "type": "webview",
          "id": "hitl-mcp.chatView",
          "name": "Chat",
          "when": "true",
          "icon": "$(comment)"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "hitl-mcp.chatSessions",
        "contents": "No chat sessions found.\n[Create New Session](command:hitl-mcp.createSession)\nTo learn more about how to use HITL MCP [read our docs](https://github.com/nix1/HITL-MCP)."
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "hitl-mcp.createSession",
          "when": "view == hitl-mcp.chatSessions",
          "group": "navigation"
        },
        {
          "command": "hitl-mcp.refreshSessions",
          "when": "view == hitl-mcp.chatSessions",
          "group": "navigation"
        },
        {
          "command": "hitl-mcp.reportIssue",
          "when": "view == hitl-mcp.chatSessions",
          "group": "navigation"
        }
      ]
    },
    "mcpServerDefinitionProviders": [
      {
        "id": "hitl-mcp.server",
        "label": "HITL MCP Server"
      }
    ],
    "configuration": {
      "title": "HITL MCP",
      "properties": {
        "hitl-mcp.logging.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable logging to .vscode directory in workspace. Logs MCP server activity and debugging information."
        },
        "hitl-mcp.logging.level": {
          "type": "string",
          "enum": [
            "ERROR",
            "WARN",
            "INFO",
            "DEBUG"
          ],
          "default": "INFO",
          "description": "Set the logging level for HITL MCP server"
        },
        "hitl-mcp.notifications.enableSound": {
          "type": "boolean",
          "default": true,
          "description": "Enable audio notifications when AI agents send messages"
        },
        "hitl-mcp.notifications.enableFlashing": {
          "type": "boolean",
          "default": true,
          "description": "Enable visual flashing notifications when AI agents send messages"
        },
        "hitl-mcp.notifications.showStartup": {
          "type": "boolean",
          "default": true,
          "description": "HITL MCP extension is new - I would appreciate any issues or suggestions logged @"
        },
        "hitl-mcp.telegram.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable Telegram notifications for AI agent messages. Requires bot token and chat ID."
        },
        "hitl-mcp.telegram.botToken": {
          "type": "string",
          "default": "",
          "description": "Telegram bot token from @BotFather. Required for Telegram integration."
        },
        "hitl-mcp.telegram.chatId": {
          "type": "string",
          "default": "",
          "description": "Telegram chat ID where messages will be sent. Get this by messaging your bot and checking /getUpdates."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "test:unit": "tsc -p . --outDir out && mocha \"out/test/chatManager.test.js\" \"out/test/mcpTools.test.js\" --ui tdd --timeout 5000"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/node-telegram-bot-api": "^0.64.13",
    "@types/vscode": "^1.105.0",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.45.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.36.0",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5.102.0",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "jsonata": "^2.1.0",
    "jsonpath-plus": "^10.3.0",
    "marked": "^18.0.2",
    "mockttp": "^4.2.1",
    "node-telegram-bot-api": "^0.67.0",
    "node-wav-player": "^1.0.0"
  }
}
